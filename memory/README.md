# Memory notes

Two notes carried over from the original setup. They are not loaded automatically
from this folder — if you want them, ask Claude to add them to this project's memory
and it will place them in the correct per-project location.

The script-read-block note only applies if you hit permission errors running the
scripts. On an unmanaged personal machine you probably won't; skip it if so.
